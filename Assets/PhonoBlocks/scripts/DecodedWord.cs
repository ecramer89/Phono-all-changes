using System.Collections;
using System.Collections.Generic;


public class DecodedWord  {
	//should save:
	//map of the rules it instantiates and the range (indeces of original word) which instantiate it

	//in other words:
	//the regex that matches (magic e, closed/open syll)
	//the match group data

	//then the list of 
}
